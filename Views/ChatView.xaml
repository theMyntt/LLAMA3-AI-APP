<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Llama_APP.Views.ChatView">
    <ContentPage.Content>
        <ScrollView VerticalOptions="FillAndExpand">
            <VerticalStackLayout>
                <Grid ColumnDefinitions="*,Auto">
                    <Entry Text="{Binding Prompt}" HorizontalOptions="FillAndExpand" Grid.Column="0">
                        <Entry.Placeholder>Digite sua mensagem</Entry.Placeholder>
                    </Entry>
                    <Button Command="{Binding GetMessages}" Grid.Column="1">
                        <Button.Text>Enviar</Button.Text>
                    </Button>
                </Grid>

                <CollectionView ItemsSource="{Binding Messages}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <VerticalStackLayout Margin="0,20,0,20">
                                <Label Text="LLAMA3: " FontAttributes="Bold" FontSize="20" />
                                <Label Text="{Binding Message}" />
                            </VerticalStackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>